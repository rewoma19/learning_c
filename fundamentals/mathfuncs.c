#include <stdio.h>
#include <stdlib.h>

// Header file needed for math functions
#include <math.h>

int main() {
  // int x = 9;
  float x = 45;

  // x = sqrt(x);
  // x = pow(x, 2);
  // x = round(x);
  // x = ceil(x);
  // x = floor(x);
  // x = abs(x);
  // x = log(x);
  // x = sin(x);
  // x = cos(x);
  x = tan(x);


  printf("%f", x);

  return 0;
}